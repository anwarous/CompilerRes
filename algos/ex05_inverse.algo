Algorithme ex05
debut
    Ecrire("Entrez un nombre : ")
    Lire(n)
    inverse <-- 0
    tant que n > 0 faire
        reste <-- n MOD 10
        inverse <-- (inverse * 10) + reste
        n <-- n DIV 10
    fin tant que
    Ecrire("Sortie : ", inverse)
fin
